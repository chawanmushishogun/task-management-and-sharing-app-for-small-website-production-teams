/** 一覧の各列の幅。担当者・期日・ステータス・備考で共通 */
export const COL_W = 160;

/** タスク名列がこれ以上縮まないようにする下限。ヘッダーと行の両方に同じ値を適用すること */
export const TASK_NAME_MIN_W = 400;

/** 全タスク画面のタスク名列の下限。プロジェクト列がある分リストビューより狭い */
export const ALL_TASKS_NAME_MIN_W = 300;

/** セクション名の左端に合わせるための字下げ: px-6(24px) + 矢印(13px) + gap-2(8px) */
export const TASK_NAME_PL = 45;
